import { headerLight } from '../components/Header/HeaderInfos';

export default {
  title: 'light',
  general: {
    color: 'var(--g3)',
    background: 'var(--g8)',
    strong: 'var(--g2)',
    links: 'var(--g12)',
    borderLinks: 'var(--g3)',
  },
  header: headerLight,
};
